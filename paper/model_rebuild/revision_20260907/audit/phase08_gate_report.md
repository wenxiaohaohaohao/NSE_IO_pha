# Phase 8 independent proposition audit — PASS

Authorization REV-EXEC-001. Exactly six propositions and one novelty corollary; zero unresolved P0 and zero phase-specific P1.

| Result | Actual assumptions / parents | Independent proof and limits |
|---|---|---|
| 1 sorting | P02 derived R_c, P03 interior technology signs, P04 finite wedge/crossing | Delta_k=s R_c c_I,k-F_I,k>0, including s=0. IVT plus strict monotonicity yields the unique conditional root. T/A dominance invalidates unconditional route inference; M=0 has no finite E cutoff. High k improves relative internal value but need not guarantee global internal choice. |
| 2 relevant set | Invariant old values, deterministic max, finite post-MAH E | max(B,E)-B=(E-B)_+. Strict iff E>B; zero otherwise. Infinite-price exclusion is pointwise, not a uniform finite price on unbounded support; dominated convergence gives expected gain ->0. |
| 3 advancement | P05 optimizer, P02/P03 for k effect, nu>=1 only for the manufacturing-response sufficient result | Strictly increasing power gives Delta x>0 iff DeltaOmega>0. a scales by a^(1/nu). Increasing old value u and decreasing surplus d reduce h(u+d)-h(u) for concave h; right continuity at the exact feasible endpoint closes the global k comparison. Strict curvature channel requires positive d. nu=1 removes baseline-value channel. No k sign for nu<1 from those inequalities alone. |
| novelty corollary | P01 exogenous mixture, common x, P2 | Linear expectation gives sum rho_g DeltaOmega_g. Swapping conditional supports reverses rankings without adding a control. A zero direct conditional gain is not a zero outcome guarantee under common x. |
| 4 CMO root | P06 primitive supply/background/envelopes and P04 no-tie | Continuity and signs of Z at zero/infinity give a root; derived strictly decreasing Z gives uniqueness. Aggregate monotonicities are derived, not primitive conclusions. |
| 5 scarcity | P4 unique root; invariant supply/background/old values | At old root Z1 equals nonnegative study demand, so p1>=p0. E value falls by b*(p1-p0), yielding pointwise positive-part bounds. Increasing advancement map preserves bounds for every nu>0, including zero corner. Zero study demand gives exactly unchanged price. Perfectly elastic supply is an alternative limiting case. |
| 6 outcomes | P01 timing and mixture, P04 routes, P05 optimizer, common exogenous s | Multiply planned mass by realization-weighted retained indicator; add/subtract a*x1*Q0 for exact decomposition. An old I project cannot move to T/A because old values do not change; hence retained mass weakly rises. Common x means positive baseline class mass receives advancement spillover. One-class zero changes remain possible when that class has zero retained realization mass; no strict original response or ranking imposed. |

## Required boundaries

PASS k->infinity: relative internal advantage improves subject to crossing, no universal selection claim. PASS internal infeasibility: infinite setup / W_I=-infinity, never evaluate undefined c_I. PASS M=0: E excluded, background CMO market survives. PASS p->infinity: each fixed project exits E, integrable envelopes control aggregates. PASS zero entrusted advantage: DeltaOmega=Delta x=0. PASS perfectly elastic supply: common price, direct and equilibrium effects coincide. PASS nu=1: linear x in Omega and no curvature-based baseline-value attenuation.

## Scope, numerical cross-checks and presentation

64 parameter cases checked concavity-based k ordering and scarcity inequalities; the nu<1 counterexample confirms the need for a sufficient condition. Four independent outcome decompositions include unchanged route weights and zero corners. These are mathematical checks, not data or calibration.

85 classified registry rows plus explicit aliases precede module use. Typed outcomes use s_g=s, preserving sum_g Y_g=Y and the valuation kernel; no optional distinct class kernel activated. No additional market/control/state, direct policy productivity/realization change, patent or welfare claim. RL01--28 pass in this phase scope; global audit remains Phase 17.

All seven pages inspected, then pages affected by the endpoint clarification re-rendered and checked. Compilation, unique labels, resolved references and protected hashes pass.

APPROVED after the current gate under REV-EXEC-001. Next: observed-outcome interpretation and identification boundaries.
