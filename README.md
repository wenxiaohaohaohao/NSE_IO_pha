# DANNI workspace

This repository manages the full `D:\working_paper\DANNI` workspace.

Top-level structure:

- `.codex/`: project-scoped Codex configuration and subagents
- `literatures/`: background papers, slides, and reference materials
- `research_notes/`: model notes, empirical notes, drafts, Stata code, and LaTeX fragments

Migration note:

- The old standalone Git metadata from `research_notes/.git` was preserved under `.repo_backup/` and is ignored by the new root repository.
